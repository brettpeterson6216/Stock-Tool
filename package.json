{
  "name": "implied-lens",
  "version": "1.2.0",
  "description": "Implied Lens — institutional-grade stock analysis platform (Node + Express + Turso + bcryptjs).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check server.js && node --check routes/auth.js && node --check routes/billing.js && node --check routes/market-data.js && node --check routes/financials.js && node --check routes/lens-score.js && node --check routes/workspace.js && node --check lib/config.js && node --check lib/db.js && node --check lib/email.js && node --check lib/plan.js && node --check lib/build-info.js && node --check lib/provider-health.js && node --check lib/acquisition-tickers.js && node --check lib/stock-research.js && node --check lib/lens-score-engine.js && node --check lib/lens-calibration.js && node --check public/model-math.js && node --check public/projection-lab.js && node --check public/product-system.js && node --check public/workspace-system.js && node --check public/research-system.js && node --check labs/lens-score/lens-score-engine.js && node --check labs/lens-score/app.js && node --check scripts/calibrate-lens-score.js && node --check scripts/verify-deploy.js && echo \"All files OK\"",
    "test": "node --test test/*.test.js",
    "verify:deploy": "node scripts/verify-deploy.js",
    "calibrate:lens": "node scripts/calibrate-lens-score.js"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "compression": "^1.8.1",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "express-session": "^1.18.0",
    "helmet": "^7.1.0",
    "resend": "^6.12.3",
    "stripe": "^16.0.0"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
